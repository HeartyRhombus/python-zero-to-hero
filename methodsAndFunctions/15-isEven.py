# Define a function called is_even that takes in one argument, and returns True if the passed-in value is even, False if it is not.
# Remember, don't run the funciton, simply provide the definition.

def is_even(arg):
    if arg % 2 == 0:
        return True
    else:
        return False